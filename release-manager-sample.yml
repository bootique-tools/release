maven-central:
  username: name
  password: password
  close: 0

preferences:
  gitHubToken: token
  basePath: path

jerseyclient:
  auth:
    bintray:
      type: basic
      username: name
      password: password

jdbc:
  mysql:
    jdbcUrl: url
    driverClassName: com.mysql.cj.jdbc.Driver
    maximumPoolSize: 1
    minimumIdle: 1
    username: name
    password: password

cayenne:
  datasource: mysql
  createSchema: true