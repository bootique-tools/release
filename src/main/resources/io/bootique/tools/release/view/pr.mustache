{{> header}}
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                <th><a href="/ui/pr?sort=title">Title</a></th>
                <th><a href="/ui/pr?sort=author">Author</a></th>
                <th><a href="/ui/pr?sort=date:desc">Created</a></th>
                <th><a href="/ui/pr?sort=labels">Labels</a></th>
                <th><a href="/ui/pr?sort=repo">Repository</a></th>
            </tr>
            </thead>
            <tbody>
            {{#pullRequests}}
                <tr>
                    <td>
                        <a href="{{url}}" target="_blank">{{title}}</a>
                        <a href="{{url}}" target="_blank" class="badge badge-secondary">#{{number}}</a>
                    </td>
                    <td><a href="/ui/pr?filter=a:{{author.login}}">{{author.login}}</a></td>
                    <td>{{createdAtStr}}</td>
                    <td>
                        {{#labels.labels}}
                            <a href="/ui/pr?filter=l:{{name}}" class="badge" style="color: white; text-decoration: none; background-color: #{{color}}">{{name}}</a>&nbsp;
                        {{/labels.labels}}
                    </td>
                    <td>
                        {{#repository}}
                        <a href="/ui/pr?filter=r:{{name}}">{{name}}</a>
                        {{#parent}}
                            <a href="{{url}}" target="_blank" class="badge badge-secondary">parent</a>
                        {{/parent}}
                        {{/repository}}
                    </td>
                </tr>
            {{/pullRequests}}
            </tbody>
        </table>
    </div>
{{> footer}}